// Ejemplo de configuración de Turso. Esta copia SÍ se puede subir a GitHub.
// Copiá este archivo a config.local.js (ignorado por git) y completá ahí tus datos reales.
window.MATES_LOCAL_DB = {
  url: "",
  token: "",
};